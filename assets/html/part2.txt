:30px 34px 24px;
  }
  .intro-item {
    display:flex;
    align-items:flex-start;
    gap:14px;
    margin:0 0 18px;
    color:#374151;
    font-size:17px;
    line-height:1.45;
  }
  .intro-item:last-child { margin-bottom:0; }
  .intro-icon {
    width:34px;
    height:34px;
    flex:0 0 34px;
    display:grid;
    place-items:center;
    font-size:23px;
    line-height:1;
    margin-top:0;
  }
  .intro-footer {
    border-top:1px solid #f1f5f9;
    padding:20px 28px 18px;
  }
  .start-btn {
    width:100%;
    border:0;
    border-radius:9px;
    background:#16a34a;
    color:#fff;
    padding:15px 18px;
    font-size:18px;
    font-weight:800;
    box-shadow:0 7px 18px rgba(22,163,74,.23);
  }
  .start-btn:hover { background:#15803d; }
  .intro-meta {
    margin-top:12px;
    text-align:center;
    color:#94a3b8;
    font-size:11px;
    line-height:1.45;
  }
  .simulator-shell { display:none; background:#fff; width:100%; height:100vh; overflow:hidden; }
  .topbar {
    height:54px;
    border-bottom:1px solid #166534;
    background:#16a34a;
    display:flex;
    align-items:center;
    justify-content:space-between;
    padding:0 16px;
  }
  .topbar-left {
    display:flex;
    align-items:center;
    gap:10px;
  }
  .topbar-title {
    font-size:14px;
    font-weight:700;
    color:#ffffff;
  }
  .topbar-subtitle {
    font-size:12px;
    color:#dcfce7;
  }
  .topbar-actions {
    display:flex;
    align-items:center;
    gap:10px;
  }
  .topbar-btn {
    border:1px solid rgba(255,255,255,.28);
    background:rgba(255,255,255,.14);
    color:#ffffff;
    border-radius:8px;
    padding:9px 12px;
    font-size:13px;
    font-weight:700;
    display:flex;
    align-items:center;
    gap:8px;
  }
  .topbar-btn:hover { background:rgba(255,255,255,.22); }
  .topbar-icon {
    display:inline-grid;
    place-items:center;
    width:20px;
  }
  body.simulation-started { background:#fff; }
  body.simulation-started .intro-screen { display:none; }
  body.simulation-started .simulator-shell { display:block; }


  .tour-layer {
    position:fixed;
    inset:0;
    z-index:9999;
    pointer-events:none;
    display:none;
  }
  .tour-layer.active { display:block; }

  .tour-highlight {
    position:fixed;
    border:3px solid #22c55e;
    border-radius:10px;
    box-shadow:0 0 0 9999px rgba(15,23,42,.58);
    transition:all .28s ease;
    pointer-events:none;
  }

  .tour-tooltip {
    position:fixed;
    width:min(360px, calc(100vw - 32px));
    background:#fff;
    border:1px solid #d1d5db;
    border-radius:12px;
    box-shadow:0 18px 50px rgba(15,23,42,.28);
    padding:18px;
    color:#334155;
    pointer-events:auto;
    transition:all .28s ease;
  }

  .tour-tooltip.centered {
    width:min(620px, calc(100vw - 40px));
    padding:26px 28px;
    text-align:center;
  }

  .tour-step {
    font-size:11px;
    font-weight:800;
    color:#16a34a;
    text-transform:uppercase;
    letter-spacing:.06em;
    margin-bottom:6px;
  }

  .tour-tooltip.centered .tour-step {
    font-size:12px;
    margin-bottom:8px;
  }

  .tour-title {
    margin:0 0 8px;
    font-size:19px;
    color:#0f172a;
  }

  .tour-tooltip.centered .tour-title {
    font-size:25px;
    margin-bottom:10px;
  }

  .tour-text {
    margin:0;
    font-size:14px;
    line-height:1.5;
    color:#475569;
  }

  .tour-tooltip.centered .tour-text {
    font-size:16px;
    line-height:1.55;
    white-space:pre-line;
    text-align:left;
  }

  .tour-actions {
    margin-top:16px;
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:10px;
  }

  .tour-progress {
    font-size:11px;
    color:#94a3b8;
  }

  .tour-tooltip.centered .tour-progress {
    font-size:13px;
  }

  .tour-next,
  .tour-finish {
    border:0;
    border-radius:8px;
    background:#16a34a;
    color:#fff;
    font-weight:800;
    padding:10px 16px;
    min-width:105px;
  }

  .tour-tooltip.centered .tour-next,
  .tour-tooltip.centered .tour-finish {
    padding:12px 20px;
    font-size:14px;
  }

  .tour-tooltip.centered .tour-skip {
    font-size:14px;
  }

  .tour-next:hover,
  .tour-finish:hover { background:#15803d; }

  .tour-skip {
    border:0;
    background:transparent;
    color:#64748b;
    font-size:12px;
    text-decoration:underline;
  }

  @media (max-width: 700px) {
    .tour-tooltip { width:calc(100vw - 24px); }
  }


  .help-popover {
    position:fixed;
    z-index:9998;
    width:min(320px, calc(100vw - 24px));
    background:#f0fdf4;
    border:1px solid #86efac;
    border-radius:12px;
    box-shadow:0 18px 50px rgba(15,23,42,.18);
    padding:16px 16px 14px;
    display:none;
  }
  .help-popover.active { display:block; }

  .help-popover::after {
    content:"";
    position:absolute;
    width:14px;
    height:14px;
    background:#f0fdf4;
    transform:rotate(45deg);
  }

  .help-popover.placement-right::after {
    left:-8px;
    top:var(--arrow-top, 24px);
    border-left:1px solid #86efac;
    border-bottom:1px solid #86efac;
  }

  .help-popover.placement-left::after {
    right:-8px;
    top:var(--arrow-top, 24px);
    border-right:1px solid #86efac;
    border-top:1px solid #86efac;
  }

  .help-popover.placement-bottom::after {
    top:-8px;
    left:var(--arrow-left, 24px);
    border-left:1px solid #86efac;
    border-top:1px solid #86efac;
  }

  .help-popover.placement-top::after {
    bottom:-8px;
    left:var(--arrow-left, 24px);
    border-right:1px solid #86efac;
    border-bottom:1px solid #86efac;
  }
  .help-badge {
    display:inline-block;
    margin-bottom:6px;
    font-size:10px;
    font-weight:800;
    color:#15803d;
    text-transform:uppercase;
    letter-spacing:.06em;
  }
  .help-title {
    margin:0 0 7px;
    font-size:17px;
    color:#0f172a;
    font-weight:800;
  }
  .help-text {
    margin:0;
    font-size:13px;
    line-height:1.45;
    color:#475569;
  }
  .help-close {
    margin-top:12px;
    border:1px solid #bbf7d0;
    border-radius:8px;
    background:#dcfce7;
    color:#166534;
    font-size:12px;
    font-weight:700;
    padding:8px 12px;
  }
  .help-close:hover { background:#bbf7d0; }


  .tour-label-green {
    color:#16a34a;
    font-weight:800;
  }


  .scenario-box {
    margin:5px 0 3px;
    border:1px solid #86efac;
    background:#e8f8ec;
    border-radius:10px;
    padding:9px 10px;
    flex:0 0 auto;
  }
  .scenario-title {
    margin:0 0 2px;
    color:#0f172a;
    font-size:14px;
    font-weight:800;
  }
  .scenario-subtitle {
    margin:0 0 7px;
    color:#475569;
    font-size:12px;
    line-height:1.25;
  }
  .response-options {
    display:flex;
    flex-direction:column;
    gap:5px;
  }
  .response-option {
    width:100%;
    text-align:left;
    border:1px solid #cbd5e1;
    background:#ffffff;
    color:#334155;
    border-radius:8px;
    padding:7px 10px;
    font-size:12px;
    line-height:1.25;
  }
  .response-option:hover {
    background:#f8fafc;
  }
  .response-option.correct {
    border-color:#22c55e;
    background:#f0fdf4;
  }
  .response-option.incorrect {
    border-color:#ef4444;
    background:#fef2f2;
  }
  .response-option:disabled {
    cursor:default;
    opacity:1;
  }


  .left, .conversations, .chat, .details { min-width:0; }
  .left *, .conversations *, .chat *, .details * { min-width:0; }

  .left { overflow:hidden; }
  .conversations { overflow:hidden; }
  .details { overflow-y:auto; overflow-x:hidden; }

  .nav button span:first-child,
  .conv-item .name,
  .conv-item .preview,
  .field span,
  .mini {
    overflow-wrap:anywhere;
  }

  @media (max-width:1180px) {
    .topbar { padding:0 10px; }
    .topbar-subtitle { display:none; }
    .topbar-actions { gap:6px; }
    .topbar-btn { padding:8px 9px; font-size:12px; }
    .brand { padding:0 10px; }
    .section-title { padding-left:10px; padding-right:10px; }
    .nav { padding:0 5px; }
    .nav button { padding:7px 6px; }
    .conv-head { padding:0 10px; }
    .tabs { gap:10px; padding:0 10px; }
    .alert { margin:8px; }
    .chat-head { padding:0 10px; }
    .chat-name { font-size:20px; }
    .messages { padding:14px 12px; }
    .composer { margin-left:8px; margin-right:8px; }
    .details { padding-left:5px; padding-right:5px; }
  }

  @media (max-width:980px) {
    .topbar-title { font-size:13px; }
    .topbar-btn span:last-child { display:none; }
    .topbar-btn { width:38px; height:38px; padding:0; justify-content:center; }
    .topbar-icon { width:auto; font-size:17px; }
    .details { flex-basis:160px; width:160px; }
    .conversations { flex-basis:190px; width:190px; }
    .left { flex-basis:135px; width:135px; }
    .bubble { max-width:82%; }
  }


  body:not(.simulation-started) {
    overflow:auto;
  }

  body.simulation-started {
    overflow:hidden;
  }

  .rail,
  .left,
  .conversations,
  .details {
    height:100%;
    min-height:0;
  }

  .left,
  .conversations {
    overflow-y:auto;
    overflow-x:hidden;
  }

  .topbar {
    flex:0 0 54px;
  }

  /* Em telas mais baixas, compacta elementos fixos para preservar o chat. */
  @media (max-height:760px) {
    .chat-head { height:50px; flex-basis:50px; }
    .statusline { height:30px; flex-basis:30px; }
    .composer-tabs { height:34px; }
    .composer-tabs b { height:34px; }
    textarea { min-height:60px; height:60px; }
    .toolbar { height:34px; }
    .hint { font-size:11px; padding-bottom:2px; }
    .messages { padding-top:12px; padding-bottom:12px; }
  }

  @media (max-height:650px) {
    .topbar { height:48px; flex-basis:48px; }
    .app { height:calc(100vh - 48px); }
    .chat-head { height:46px; flex-basis:46px; }
    .statusline { height:28px; flex-basis:28px; }
    textarea { min-height:48px; height:48px; }
    .composer { margin-bottom:4px; }
  }


  @media (max-width:980px) {
    .rail {
      width:44px;
      flex-basis:44px;
      padding-left:3px;
      padding-right:3px;
    }
    .logo,
    .logo img {
      width:30px;
      height:30px;
    }
    .rail button,
    .rail .rail-btn {
      width:28px;
      height:28px;
    }
    .rail-user {
      width:34px;
      min-height:42px;
      font-size:10px;
    }
  }


  .typing-indicator {
    width:max-content;
    display:inline-flex;
    align-items:center;
    padding:8px 11px;
    border-radius:8px;
    background:#f8fafc;
    border:1px solid #cbd5e1;
    margin:1px 0 8px;
  }
  .typing-dots {
    display:inline-flex;
    align-items:center;
    gap:4px;
    height:8px;
  }
  .typing-dots span {
    width:6px;
    height:6px;
    border-radius:999px;
    background:#94a3b8;
    display:block;
    animation:typingBounce 1s infinite ease-in-out;
  }
  .typing-dots span:nth-child(2) { animation-delay:.15s; }
  .typing-dots span:nth-child(3) { animation-delay:.30s; }

  @keyframes typingBounce {
    0%, 60%, 100% { transform:translateY(0); opacity:.55; }
    30% { transform:translateY(-3px); opacity:1; }
  }


  /* Alternativas mais compactas e próximas ao campo de resposta */
  .messages > .scenario-box:last-child {
    margin-bottom:2px;
  }
  @media (max-height:760px) {
    .scenario-box {
      padding:7px 8px;
      margin-bottom:2px;
    }
    .scenario-title { font-size:13px; }
    .scenario-subtitle { margin-bottom:5px; font-size:11px; }
    .response-options { gap:4px; }
    .response-option { padding:6px 8px; font-size:11px; line-height:1.2; }
  }


  .double-check {
    display:inline-block;
    color:#2563eb !important;
    font-weight:700;
    letter-spacing:-4px;
    margin-right:5px;
    white-space:nowrap;
  }
  .stamp .double-check {
    color:#2563eb !important;
  }


  .tab-static {
    cursor:default !important;
  }
  #composer[readonly] {
    cursor:default;
    user-select:none;
  }


  [data-help-title] {
    cursor:pointer !important;
  }

  .tab-static[data-help-title] {
    cursor:pointer !important;
  }


  .feedback-overlay {
    position:fixed;
    inset:0;
    background:rgba(15,23,42,.58);
    display:none;
    align-items:center;
    justify-content:center;
    z-index:5000;
    padding: