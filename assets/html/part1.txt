<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Protótipo Octa Conversas</title>
<style>
  * { box-sizing: border-box; }
  html, body { margin:0; width:100%; height:100%; background:#fff; font-family:Arial,Helvetica,sans-serif; color:#334155; overflow:hidden; }
  body { overflow:hidden; }
  button, textarea { font:inherit; }
  button { cursor:pointer; }
  .app { width:100%; min-width:0; height:calc(100vh - 54px); min-height:0; display:flex; background:#fff; overflow:hidden; }
  .rail {
    width:48px;
    flex:0 0 48px;
    border-right:1px solid #d9ddd6;
    background:#f3f4ef;
    display:flex;
    flex-direction:column;
    align-items:center;
    padding:8px 4px;
    gap:10px;
  }
  .rail-top,
  .rail-bottom {
    width:100%;
    display:flex;
    flex-direction:column;
    align-items:center;
    gap:10px;
  }
  .rail button,
  .rail .rail-btn {
    width:30px;
    height:30px;
    border:0;
    border-radius:8px;
    background:transparent;
    color:#6b7280;
    display:grid;
    place-items:center;
    position:relative;
    padding:0;
  }
  .rail button:hover,
  .rail .rail-btn:hover {
    background:#e6e7e1;
  }
  .rail .active {
    background:#dcded7;
    color:#2f3340;
  }
  .rail-btn svg {
    width:17px;
    height:17px;
    stroke:currentColor;
    fill:none;
    stroke-width:2;
    stroke-linecap:round;
    stroke-linejoin:round;
  }
  .rail-btn .fill-icon {
    fill:currentColor;
    stroke:none;
  }
  .logo {
    width:34px;
    height:34px;
    border-radius:9px;
    display:grid;
    place-items:center;
    background:transparent;
    overflow:hidden;
  }
  .logo img {
    width:34px;
    height:34px;
    display:block;
    border-radius:9px;
  }
  .spacer { flex:1; }
  .notification-badge {
    position:absolute;
    right:-2px;
    bottom:-2px;
    min-width:12px;
    height:12px;
    border-radius:999px;
    background:#d81b60;
    color:#fff;
    font-size:8px;
    font-weight:700;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:0 3px;
    border:1px solid #f3f4ef;
  }
  .rail-user {
    width:36px;
    cursor:pointer;
    min-height:44px;
    background:#e3e4dd;
    border-radius:10px;
    display:flex;
    flex-direction:column;
    align-items:center;
    justify-content:center;
    font-size:11px;
    font-weight:700;
    color:#334155;
    position:relative;
    line-height:1;
    padding-top:4px;
  }
  .rail-user-status {
    width:14px;
    height:14px;
    border-radius:999px;
    background:#22c55e;
    border:2px solid #f3f4ef;
    position:absolute;
    right:-2px;
    bottom:-2px;
  }
  .left { width:clamp(145px, 15vw, 190px); flex:0 0 clamp(145px, 15vw, 190px); min-width:0; border-right:1px solid #e5e7eb; background:#fff; display:flex; flex-direction:column; }
  .brand { height:58px; display:flex; align-items:center; padding:0 14px; font-weight:700; color:#0f172a; font-size:16px; }
  .section-title { display:flex; justify-content:space-between; padding:10px 14px 6px; font-weight:700; color:#334155; font-size:16px; }
  .nav { padding:0 7px; }
  .nav button { width:100%; border:0; background:#fff; border-radius:7px; padding:8px 8px; display:flex; justify-content:space-between; color:#475569; font-size:14px; text-align:left; }
  .nav button:hover, .nav button.active { background:#f8fafc; color:#0f172a; font-weight:700; }
  .agent { margin-top:auto; border-top:1px solid #e5e7eb; padding:12px 14px; font-size:13px; }
  .agent b { display:block; color:#0f172a; }
  .agent span { color:#16a34a; }

  .conversations { width:clamp(210px, 22vw, 300px); flex:0 0 clamp(210px, 22vw, 300px); min-width:0; border-right:1px solid #e5e7eb; background:#fff; display:flex; flex-direction:column; }
  .conv-head { height:58px; display:flex; align-items:center; justify-content:space-between; padding:0 15px; font-weight:700; color:#0f172a; font-size:16px; }
  .tabs { height:40px; border-bottom:1px solid #e5e7eb; display:flex; gap:18px; align-items:end; padding:0 15px; }
  .tabs button { height:40px; border:0; background:#fff; color:#64748b; font-size:13px; border-bottom:2px solid transparent; }
  .tabs button.active { color:#0f172a; border-bottom-color:#0f172a; font-weight:700; }
  .alert { margin:12px; border:1px solid #fda4af; background:#fff7f7; color:#be123c; border-radius:6px; padding:8px 10px; font-size:16px; }
  .conv-item { width:100%; border:0; border-left:4px solid #38bdf8; background:#f7fcff; padding:11px 12px 22px; text-align:left; position:relative; }
  .conv-item .tag { display:inline-block; background:#e5e7eb; color:#475569; border-radius:4px; font-size:13px; padding:2px 5px; margin-bottom:5px; }
  .conv-item .name { font-weight:600; color:#334155; font-size:16px; }
  .conv-item .preview { margin-top:5px; padding-right:42px; color:#64748b; font-size:13px; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
  .conv-item .preview-time { position:absolute; right:10px; bottom:6px; color:#94a3b8; font-size:11px; font-weight:500; line-height:1; }

  .chat { flex:1 1 auto; min-width:0; min-height:0; height:100%; display:flex; flex-direction:column; background:#fff; overflow:hidden; }
  .chat-head { height:58px; flex:0 0 58px; border-bottom:1px solid #e5e7eb; display:flex; align-items:center; padding:0 16px; }
  .avatar { width:30px; height:30px; border-radius:6px; background:#e5e7eb; display:grid; place-items:center; font-size:13px; margin-right:9px; }
  .chat-name { font-size:24px; color:#0f172a; font-weight:500; }
  .chat-actions { margin-left:auto; display:flex; gap:16px; }
  .chat-actions button { border:0; background:#fff; color:#64748b; font-size:19px; }
  .statusline { height:34px; flex:0 0 34px; border-bottom:1px solid #e5e7eb; display:flex; align-items:center; padding:0 16px; font-size:13px; color:#64748b; }
  .statusline .center { margin:auto; }
  .messages { flex:1 1 auto; min-height:0; overflow-y:auto; overflow-x:hidden; padding:14px 20px 5px; background:#fff; display:flex; flex-direction:column; }
  .messages::before { content:''; flex:1 0 auto; min-height:0; }
  .messages:has(.bubble:nth-of-type(n+8))::before { flex-grow:0; }
  .bubble { max-width:340px; border-radius:8px; padding:9px 11px; line-height:1.32; font-size:14px; margin-bottom:10px; }
  .bubble.client { background:#f8fafc; border:1px solid #cbd5e1; }
  .bubble.agent { margin-left:auto; background:#f0f9ff; border:1px solid #bae6fd; }
  .stamp { text-align:right; margin-top:5px; color:#64748b; font-size:11px; }
  .system { text-align:center; color:#64748b; font-size:12px; margin:3px 0 10px; }

  .composer { flex:0 0 auto; margin:0 14px 5px; border:1px solid #cbd5e1; border-radius:10px; overflow:hidden; background:#fff; }
  .composer-tabs { height:40px; border-bottom:1px solid #e5e7eb; display:flex; align-items:center; gap:20px; padding:0 12px; font-size:13px; }
  .composer-tabs b { height:40px; display:flex; align-items:center; border-bottom:2px solid #334155; }
  .composer-tabs span { color:#64748b; }
  .summary { background:#7c3aed; color:#fff !important; border-radius:5px; padding:5px 9px; }
  textarea { width:100%; min-height:86px; resize:none; border:0; outline:none; padding:12px; font-size:16px; color:#334155; background:#fff; }
  .toolbar { height:38px; border-top:1px solid #e5e7eb; display:flex; align-items:center; gap:14px; padding:0 12px; }
  .toolbar button { border:0; background:#fff; color:#64748b; }
  .toolbar .send { margin-left:auto; color:#38bdf8; font-size:24px; }
  .hint { flex:0 0 auto; text-align:center; font-size:13px; color:#64748b; padding-bottom:4px; }

  .details {
    width:clamp(205px, 19vw, 250px);
    flex:0 0 clamp(205px, 19vw, 250px);
    min-width:0;
    border-left:1px solid #e5e7eb;
    background:#f7f8fa;
    padding:0;
    overflow-y:auto;
    overflow-x:hidden;
  }

  .details-top {
    background:#fff;
    padding:10px 12px 12px;
    border-bottom:1px solid #e5e7eb;
  }

  .details-title {
    display:flex;
    align-items:center;
    gap:7px;
    color:#1f2937;
    font-size:16px;
    font-weight:800;
    margin-bottom:10px;
  }

  .details-status {
    border:1px solid #cbd5e1;
    background:#eef0f3;
    color:#1f2937;
    border-radius:7px;
    padding:7px 10px;
    font-size:13px;
    font-weight:700;
    display:inline-flex;
    align-items:center;
    gap:20px;
    margin-bottom:10px;
  }

  .details-summary {
    display:grid;
    gap:7px;
    font-size:14px;
    color:#475569;
  }

  .details-summary b {
    color:#253047;
    font-size:14px;
  }

  .details-card {
    background:#fff;
    border-top:1px solid #e5e7eb;
    border-bottom:1px solid #e5e7eb;
    margin:7px 0;
    overflow:hidden;
  }

  .details-head {
    width:100%;
    border:0;
    background:#fff;
    padding:11px 12px;
    display:flex;
    align-items:center;
    justify-content:space-between;
    text-align:left;
    color:#1f2937;
    font-size:14px;
    font-weight:800;
  }

  .details-content {
    padding:0 11px 8px;
  }

  .details-label {
    display:block;
    color:#1f2937;
    font-size:14px;
    font-weight:800;
    margin:8px 0 4px;
  }

  .details-value {
    color:#334155;
    font-size:14px;
    line-height:1.35;
    word-break:break-word;
  }

  .details-field {
    padding:7px 0 8px;
    border-bottom:1px solid #edf0f3;
  }

  .details-field:last-child {
    border-bottom:0;
  }

  .details-add {
    color:#374151;
    font-size:14px;
  }

  .details-tags {
    display:flex;
    flex-wrap:wrap;
    gap:4px;
  }

  .details-tag {
    background:#f0f2f5;
    color:#64748b;
    border-radius:6px;
    padding:4px 7px;
    font-size:11px;
    line-height:1;
    max-width:100%;
    overflow:hidden;
    text-overflow:ellipsis;
    white-space:nowrap;
  }

  .details-tag-add {
    border:1px dashed #cbd5e1;
    background:#fff;
    color:#64748b;
  }

  .origin-row {
    display:flex;
    align-items:center;
    gap:6px;
  }

  .origin-icon {
    width:18px;
    height:18px;
    border-radius:50%;
    background:#6d3bc1;
    color:#fff;
    display:grid;
    place-items:center;
    font-size:11px;
  }

  .lead-choice {
    display:flex;
    gap:8px;
    align-items:center;
    color:#334155;
    font-size:14px;
  }

  .radio-dot {
    width:13px;
    height:13px;
    border-radius:50%;
    border:2px solid #64748b;
    display:inline-block;
    position:relative;
    vertical-align:-2px;
    margin-right:3px;
  }

  .radio-dot.selected::after {
    content:'';
    position:absolute;
    inset:2px;
    border-radius:50%;
    background:#64748b;
  }

  .details-section-line {
    display:flex;
    align-items:center;
    justify-content:space-between;
    font-size:14px;
    font-weight:700;
    color:#273449;
    padding:8px 0;
    border-top:1px solid #edf0f3;
  }

  .details::-webkit-scrollbar { width:6px; }
  .details::-webkit-scrollbar-thumb { background:#d5d9df; border-radius:999px; }
  .details::-webkit-scrollbar-track { background:transparent; }

  @media (max-width:1180px) {
    .details {
      width:190px;
      flex-basis:190px;
    }
  }

  @media (max-width:980px) {
    .details {
      width:175px;
      flex-basis:175px;
    }
    .details-head { padding-left:9px; padding-right:9px; }
    .details-content { padding-left:9px; padding-right:9px; }
  }

  

  .intro-screen {
    min-height:100vh;
    background:#1f2937;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:24px;
  }
  .intro-wrap {
    width:min(760px, 100%);
    text-align:center;
  }
  .intro-title {
    margin:0;
    color:#fff;
    font-size:42px;
    line-height:1.05;
    font-weight:800;
    text-transform:uppercase;
  }
  .intro-subtitle {
    margin:8px 0 26px;
    color:#4ade80;
    font-size:22px;
    font-weight:600;
  }
  .intro-card {
    background:#fff;
    border-radius:18px;
    box-shadow:0 24px 70px rgba(0,0,0,.28);
    overflow:hidden;
    text-align:left;
  }
  .intro-content {
    padding: